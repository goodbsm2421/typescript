{
    // Array
    const fruits: string[] = ['apple', 'banana']
    const scroes: number[] = [1, 2, 3] // Array<number> 가능

    // Tuple ( 권장하지않음 )
    let student: [string, number];
    student = ['name', 1]
    student[0] // name
    student[1] // 123
}